@model IEnumerable<AgenciaViajesSpainIsDiferent.Models.Incidencias>

@{
    ViewBag.Title = "Incidencias";
}

<h2>Incidencias</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.recurso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.compañia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.recurso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.compañia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idIncidencia }) |
            @Html.ActionLink("Details", "Details", new { id=item.idIncidencia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idIncidencia })
        </td>
    </tr>
}

</table>
